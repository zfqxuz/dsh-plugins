# Example source-mount overlay. run-headless-test.sh generates an absolute-path
# version automatically; this file is kept as a minimal reference.
- insert:
    - id: session-reader
      name: ./index.js
